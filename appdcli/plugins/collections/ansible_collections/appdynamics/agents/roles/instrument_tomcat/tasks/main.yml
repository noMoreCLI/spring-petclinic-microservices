- ansible.builtin.import_tasks: check_tomcat.yml

# include this role to verify java agent after app restart
#- include_role:
#    name: appdynamics.agents.verify_java_restart
#  when: restart_app | bool

- ansible.builtin.import_tasks: app_add_user_to_group.yml
  when: > 
    create_appdynamics_user and 
    app_user is defined

- ansible.builtin.import_tasks: instrument_tomcat.yml

- ansible.builtin.import_tasks: app_service_override.yml
  when: tomcat_service is defined and add_service_override