# To install Cluster Agent
installClusterAgent: true

# Controller info
controllerInfo:
  url: "{{ controller_url }}"
  account: "{{ controller_account }}"
  username: "{{ controller_username }}"
  password: "{{ controller_password }}"
  accessKey: "{{ controller_access_key }}"

# Cluster agent config
clusterAgent:
  appName: "{{ app_name }}-Cluster"
  nsToMonitorRegex: "{{ namespace_to_monitor }}"
  logProperties:
    logLevel: INFO
    stdoutLogging: true

# Auto-instrumentation config
instrumentationConfig:
  enabled: true
  instrumentationMethod: Env
  nsToInstrumentRegex: "{{ namespace_to_instrument }}"
  defaultAppName: "{{ app_name }}"
  tierNameStrategy: manual
  enableInstallationReport: true
  imageInfo:
    java:
      image: "docker.io/appdynamics/java-agent:latest"
      agentMountPath: /opt/appdynamics
      imagePullPolicy: Always
  instrumentationRules:
    - namespaceRegex: "{{ namespace_to_instrument }}"
      language: java
      imageInfo:
        image: "docker.io/appdynamics/java-agent:latest"
        agentMountPath: /opt/appdynamics
        imagePullPolicy: Always
