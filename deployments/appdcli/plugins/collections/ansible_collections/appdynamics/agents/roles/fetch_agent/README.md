# Fetches Agent from input location

## Variables 
Arguments to be given:

agent_destination_path (Desired destination of Agent file on the managed node)

agent_source_location (Location of Agent, File path if local or NFS, URL if its http)

fetch_protocol (How to fetch the agent: http/local/nfs)

