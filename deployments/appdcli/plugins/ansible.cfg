[defaults]
inventory = hosts
remote_user = root
host_key_checking = False
retry_files_enabled = False
roles_path = roles
collections_path = collections

[privilege_escalation]
become = True
become_method = sudo

[ssh_connection]
pipelining = True
